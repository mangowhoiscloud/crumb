{
  "name": "crumb",
  "version": "0.2.0",
  "description": "베이글코드 신작팀 멀티 에이전트 협업 도구 — Gemini CLI entry. /crumb <자연어 goal>. Adds 7 NL-triggered MCP tools (preset / status / explain / suggest / debug / doctor / export) so the user can ask in natural Korean or English.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "crumb": {
      "command": "npx",
      "args": ["tsx", "${workspacePath}/src/mcp-server.ts"],
      "cwd": "${workspacePath}",
      "env": {
        "CRUMB_AMBIENT_HARNESS": "gemini-cli",
        "CRUMB_REPO_ROOT": "${workspacePath}"
      }
    }
  },
  "excludeTools": []
}
