# --standard (default): Claude Code + Codex

[agents.coordinator]
adapter = "claude-local"
model = "claude-haiku-4-5"

[agents.planner-lead]
adapter = "claude-local"
model = "claude-sonnet-4-6"

[agents.engineering-lead]
adapter = "codex-local"
model = "gpt-5.5"

[agents.builder-fallback]
adapter = "claude-local"
model = "claude-sonnet-4-6"
